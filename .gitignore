# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual env
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# 业务数据 (运行时产生, 不入库)
data/agent_state/
data/downloads/
data/knowledge/
data/*.db
data/*.sqlite
data/*.sqlite3

# 临时目录 (开发用, 启动时生成)
tmp/
.agent_tmp/

# 上传临时文件
.env.tmp
.env.bak

# 日志
*.log
logs/

# 测试数据
tests/fixtures/*.pdf.bak

# 临时上传 (运行时产生)
data/uploads/

# API schema 导出 (运行时生成)
openapi.json


# 业务文档 (本地学习资产 + RAG 优化报告, 不入库)
docs/

# 真实环境变量 (含 API key, 不入库)
.env


# 测试 (本地跑, 不入库, 跟标准 agent 流程一致)
tests/

# 一次性脚本 (本地跑, 不入库, 跟 tests/ 一样)
scripts/
