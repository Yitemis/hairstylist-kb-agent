# ============================================================
# 美发智能知识助手 - 企业级依赖清单
# ============================================================
# 安装方式：pip install -r requirements.txt

# --- Agent 框架核心 ---
# 项目底座：多 Agent 编排、工具调用、记忆、流式输出
# 源码：https://github.com/agentscope/agentscope
agentscope>=0.8.0

# --- Web 服务 ---
# FastAPI + Uvicorn: 高性能异步服务框架
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
n# --- 缓存 ---
# Redis (生产缓存) - 可选, 没装则 fallback 到内存 LRU
redis>=5.0.0

# --- 向量库 (P2-基础设施: pgvector 替代 Milvus) ---
# pgvector 是 PostgreSQL 扩展, 跟业务库同实例, 无需独立部署
# pip install pgvector (Python 客户端, SQLAlchemy 集成)
pgvector>=0.3.0

# --- 旧 Milvus 客户端 (deprecated, 仅供回滚, 新部署忽略) ---
# pymilvus>=2.3.0

# --- LLM / Embedding / Rerank 客户端 ---
# OpenAI 兼容客户端：Embedding、Chat、VLM 调用
openai>=1.10.0
# 异步 HTTP 客户端：Rerank API 及远程模型调用
httpx>=0.26.0
# 同步 HTTP：文件下载等
requests>=2.31.0

# --- 文档解析（多格式知识入库） ---
python-docx>=1.1.0      # .docx 解析
openpyxl>=3.1.0         # .xlsx 解析
PyMuPDF>=1.23.0         # PDF 解析（fitz）
pypdf>=4.0.0            # PDF 文本抽取
markdown>=3.5.0         # Markdown 解析
Pillow>=10.0.0          # 图片处理（VLM 图像解析）
chardet>=5.2.0          # 文本编码检测
json-repair>=0.25.0     # 修复 LLM 输出的非法 JSON

# --- 检索：分词与关键词召回 ---
jieba>=0.42.1           # 中文分词
rank-bm25>=0.2.2        # BM25 关键词检索
tiktoken>=0.6.0         # Token 计数与切分

# --- 数据库与认证 ---
sqlalchemy>=2.0.0        # ORM（SQLite 开发 / MySQL 生产）
aiosqlite>=0.19.0        # SQLite 异步驱动（开发默认）
aiomysql>=0.2.0          # MySQL 异步驱动（生产用）
bcrypt>=4.0.0,<5.0.0     # 密码哈希（5.x 与 passlib 不兼容，故直接用）
python-jose[cryptography]>=3.3.0  # JWT 令牌
email-validator>=2.1.0   # 手机号/邮箱校验（pydantic 用）

# --- 配置管理 ---
python-dotenv>=1.0.0

# --- 监控与可观测性（生产环境） ---
prometheus-client>=0.19.0

# --- 测试依赖（开发环境） ---
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.1.0

# --- 数据库迁移 ---
alembic>=1.13.0
json-repair>=0.25.0  # for LLM 输出容错
