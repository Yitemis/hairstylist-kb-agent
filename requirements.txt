# ============================================================
# 美发知识助手 - 依赖清单
# ============================================================

# --- Agent 框架 ---
# 方式一（推荐，便于同时阅读框架源码）：以可编辑模式安装本地框架源码
#   pip install -e e:/agentscope-main
# 方式二：从 PyPI 安装
#   agentscope>=2.0
#
# 说明：这里不直接写 agentscope，避免和本地可编辑安装冲突。
# 请按 README 的“快速开始”单独安装框架。

# --- 配置管理 ---
python-dotenv>=1.0.0

# --- 向量库（入门用 Qdrant 内存版，零配置） ---
qdrant-client>=1.7.0

# --- Web 后端（M6 阶段用） ---
# fastapi
# uvicorn

# --- 后续阶段按需添加 ---
# 多格式解析: pypdf / python-docx / openpyxl / python-pptx
# Rerank: 视选型而定
